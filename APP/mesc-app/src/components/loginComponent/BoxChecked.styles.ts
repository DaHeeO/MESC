import styled from 'styled-components/native';
import {colors} from '../common/theme';

export const BoxChecked = styled.View`
  width: 12px;
  height: 12px;
  border: 1px solid ${colors.lightiris};
  border-radius: 4px;
  display: flex;
  justify-content: center;
  align-items: center;
`;
